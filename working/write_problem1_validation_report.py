"""由已计算的检验结果生成中文说明和论文正文，避免手工抄写数值。"""
import json
from pathlib import Path
import pandas as pd

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "results" / "第一问模型检验"
s = json.loads((OUT/"有效性检验摘要.json").read_text(encoding="utf-8"))
a = pd.read_csv(OUT/"随机扰动汇总.csv")
b = pd.read_csv(OUT/"系统性偏差压力检验.csv")
e = pd.read_csv(OUT/"效率假设检验.csv")
r = s["基准独立回代"]


def table(headers, rows):
    return "\n".join(["| "+" | ".join(headers)+" |", "|"+"---|"*len(headers)]
        + ["| "+" | ".join(map(str,row))+" |" for row in rows])


valid = [["母线平衡最大残差", f"{r['平衡最大残差_千瓦时']:.3e} 千瓦时", "不超过十万分之一千瓦时"],
    ["母线平衡均方根残差", f"{r['平衡均方根残差_千瓦时']:.3e} 千瓦时", "仅作为物理平衡误差"],
    ["储能递推最大残差", f"{r['储能递推最大残差_千瓦时']:.3e} 千瓦时", "不超过十万分之一千瓦时"],
    ["功率、容量、非负性及弃光边界违反量", f"{r['最大边界违反量_千瓦时']:.3e} 千瓦时", "不超过十万分之一千瓦时"],
    ["日末储能偏差", f"{r['终端偏差_千瓦时']:.3e} 千瓦时", "不超过十万分之一千瓦时"],
    ["同时充放电时段数", str(r['同时充放电时段数']), "等于零"],
    ["全天能量恒等式残差", f"{r['全天能量恒等式残差_千瓦时']:.3e} 千瓦时", "不超过十万分之一千瓦时"]]
rand = [[v["扰动类型"],f"{v['扰动幅度_百分比']:.0f}%",
    f"{v['成本变化均值_百分比']:+.3f}%",
    f"[{v['成本变化百分位2.5']:+.3f}%, {v['成本变化百分位97.5']:+.3f}%]",
    f"{v['最大绝对成本变化_百分比']:.3f}%",f"{v['最小节费率_百分比']:.3f}%"] for _,v in a.iterrows()]
exe = [[v["扰动类型"],f"{v['扰动幅度_百分比']:.0f}%",
    f"{v['平均电池调度变化_百分比']:.3f}%",f"{v['全冻结平均缺电量_千瓦时']:.3f}",
    f"{v['固定电池可行率_百分比']:.0f}%",
    '不适用' if pd.isna(v['可行情景平均后悔率_百分比']) else f"{v['可行情景平均后悔率_百分比']:.3f}%"] for _,v in a.iterrows()]
stress = [[v["情景"],f"{v['重优化成本_元']:.4f}",f"{v['成本变化率_百分比']:+.3f}%",
    f"{v['同情景节费率_百分比']:.3f}%"] for _,v in b.iterrows()]
eff = [[f"{v['单程效率']:.6f}",f"{v['往返效率']:.4f}",f"{v['成本_元']:.4f}",
    f"{v['相对主方案成本变化_百分比']:+.3f}%"] for _,v in e.iterrows()]
ind = a[(a['扰动类型']=='逐时段独立')&(a['扰动幅度_百分比']==10)].iloc[0]
block = a[(a['扰动类型']=='一小时相关块')&(a['扰动幅度_百分比']==10)].iloc[0]
bad = b[b['情景']=='负荷增加且光伏减少百分之十'].iloc[0]
alt = e.iloc[3]

md = rf"""# 第一问数学模型有效性检验、改进方向与鲁棒性分析

本报告基于附件一的144个时段，重新求解现有含互斥约束的混合整数线性规划，并核验原保存调度结果。模型在给定数据和既定假设下通过物理可行性与最优性检验；随机小扰动下的成本稳定性不等于既定计划无需调整即可执行。

## 一、模型类型与检验设计

第一问是确定性单日储能调度优化问题，不是预测或分类评价问题。没有待训练的预测器，也没有类别真值，因此不采用五折交叉验证或分类一致性系数。将一天随机分成五折还会破坏储能状态连续性和首末电量约束。本次的均方根残差只衡量能量平衡，不代表预测准确率。

采用五组证据：原始结果复现、全部物理约束独立回代、独立建立的连续松弛下界、无储能经济性对照、随机及系统性扰动下的重优化和执行分析。各情景使用相同容量、功率、效率和终端条件。

主方案取单程充放电效率均为0.9，允许免费弃光、不允许售电，不设置额外的电网接入容量上限；时段长度为六分之一小时，储能上下限为1200与10800千瓦时，初末储电量均为6000千瓦时。

## 二、有效性检验

### 2.1 物理约束独立回代

由原始功率重新计算 $L_t=P_t^L/6$、$R_t=P_t^{{PV}}/6$，定义

$$r_t^B=g_t+R_t+d_t-L_t-c_t-w_t,$$
$$r_t^E=E_t-E_{{t-1}}-eta_{{ch}}c_t+d_t/eta_{{dis}}.$$

检查两类残差的最大绝对值、各变量非负性、弃光不超过可用光伏、充放电功率上限、储能上下界、充放电互斥和首末电量相等。数值可行性容差预设为 $10^{{-5}}$ 千瓦时；该阈值是数值计算容差，不能代替仪表精度或工程验收标准。

{table(['检验项目','实测结果','判据'],valid)}

另检查全天能量恒等式

$$\sum_tg_t=\sum_t(L_t-R_t+w_t)+(1-eta_{{ch}})\sum_tc_t+(1/eta_{{dis}}-1)\sum_td_t.$$

该式利用首末储能相等，将购电、净负荷及充放电损耗联系起来，可检查效率是否重复计入、功率电量换算和变量符号是否错误。以上检验全部通过；已有保存结果也通过独立回代，重新求解与原调度的购电成本差为 {s['原结果成本复现差_元']:.3e} 元。

### 2.2 最优性与经济性

独立编写只含购电、充电、放电、弃光与储电量五组连续变量的线性规划，删除充放电互斥限制，保留能量平衡、储能递推、功率容量边界和日末条件。松弛可行域包含原模型可行域，因此其最优值构成原问题的成本下界。

| 指标 | 本次结果 |
|---|---:|
| 无储能购电成本 | {s['无储能成本_元']:.8f} 元 |
| 独立连续松弛下界 | {s['独立线性松弛下界_元']:.8f} 元 |
| 最终两阶段调度购电成本 | {s['基准成本_元']:.8f} 元 |
| 最终成本与下界之差 | {s['下界绝对差_元']:.8f} 元 |
| 相对下界差 | {s['下界相对差_百分比']:.3e}% |
| 相对无储能节费率 | {s['节费率_百分比']:.4f}% |

第一阶段最小化购电费，第二阶段在允许购电费增加 $10^{{-5}}$ 元的范围内最小化电池吞吐量。上述成本差正好对应这一容差，低于本次设置的 $10^{{-4}}$ 元最优性检验阈值，支持最终方案在数值容差内全局最优。下界数值本身仍受浮点计算精度影响。无储能对照证明经济收益，不能单独证明最优性。

**有效性结论：** 对附件一数据和当前物理、结算假设，模型可行、求解结果可复现且在给定容差内最优；尚不能据此认定真实预测没有误差或所有实际工况均适用。

## 三、鲁棒性分析

### 3.1 扰动生成与统计口径

保持电价不变，对负荷和光伏功率分别施加相对扰动：

$$\widetilde P_t^L=P_t^L(1+\alpha\xi_t^L),\qquad
\widetilde P_t^{{PV}}=P_t^{{PV}}(1+\alpha\xi_t^{{PV}}),\qquad
\xi\sim U[-1,1].$$

分别取 $\alpha=5\%,10\%,20\%$。例如“10%噪声”明确指扰动范围为原值的正负10%，而非标准差为10%；对应相对标准差约为5.77%。采用乘性扰动，夜间零光伏保持为零，各功率保持非负。

逐时段独立组对每个时段独立抽样；一小时相关块组在连续六个时段内使用相同相对误差，模拟持续性偏差。两组均对负荷、光伏分别独立抽样，不声称已拟合真实联合误差分布。随机种子为20260911，每种结构、每个幅度运行100个情景，共600个；同结构不同幅度使用相同基础随机数，便于比较。

报告成本变化率 $100(C_\omega^*-C^*)/C^*$、同情景无储能节费率，以及样本2.5%和97.5%分位区间。该区间是模拟结果的经验分位区间，不是均值置信区间，也不是真实运行风险保证。

### 3.2 输入变化后的重新优化

{table(['扰动结构','幅度','成本变化均值','成本变化经验95%区间','最大绝对成本变化','最小节费率'],rand)}

600个随机情景均通过求解和独立回代，最大回代违反量为 {a['最大回代违反量_千瓦时'].max():.3e} 千瓦时。10%逐时段独立扰动下，最大绝对成本变化为 {ind['最大绝对成本变化_百分比']:.3f}%；10%一小时相关块扰动下为 {block['最大绝对成本变化_百分比']:.3f}%。持续性误差的影响应与独立误差分开评价，不能仅依据独立噪声得出“鲁棒性强”。

这里每个情景都在预先知道整条扰动路径后重新优化，属于完全信息下的敏感性检验。在电网可随时补给且没有额外接入容量限制的假设下，可行性较易保持；这不能证明实际日前固定计划能应对相同扰动。

### 3.3 原计划执行检验

第一种口径固定原购电、充电、放电和弃光量。对实际扰动路径回代能量平衡，将负残差绝对值累加，得到“全冻结缺电量”；正残差另记录为富余量。缺电量表示需要调整的供需缺口，并非已经模拟了真实停电。

第二种口径只固定原电池充放电量，允许外部电网按本情景原电价即时调整、允许弃光，计算

$$\widetilde g_t=[\widetilde L_t-\widetilde R_t+c_t^0-d_t^0]_+,
\qquad \widetilde w_t=[-(\widetilde L_t-\widetilde R_t+c_t^0-d_t^0)]_+.$$

还必须检查 $\widetilde w_t\le\widetilde R_t$，否则原电池放电可能超过可吸收负荷，且在不允许售电时不可执行。可行情景的后悔率定义为 $100(C_\omega^{{fix}}-C_\omega^*)/C_\omega^*$，与同情景完全信息最优值比较。不可行情景的补救成本和后悔率留空，不能混入平均值。

{table(['扰动结构','幅度','电池调度平均变化','全冻结平均缺电量（千瓦时）','固定电池可行率','可行情景平均后悔率'],exe)}

**执行层面的主要发现：** 10%独立扰动下，固定电池方案在100个情景中均不可行；10%一小时相关块扰动下仅13个情景可行。原因是某些原本依靠电池供电的时段在负荷降低后出现过量放电，而当前模型禁止售电、也没有任意丢弃电池放电的变量。此时仅增加或减少购电不能恢复平衡，必须允许调整电池放电。可行情景的低后悔率不能掩盖其余情景的不可执行性，也不能与全体情景平均性能混淆。不可行是针对本次返回的特定最优调度，并非证明所有等价最优调度都具有相同可行率。

其中电池调度变化为 $100\sum_t(|c_t^\omega-c_t^0|+|d_t^\omega-d_t^0|)/\sum_t(c_t^0+d_t^0)$。同价时段可能存在多个等价最优解，因此调度向量变化较大不一定意味着成本不稳定。

**该补救机制是检验用假设：** 不额外计入应急购电溢价、调整惩罚或退购费用，不能作为后续问题结算结果。后悔率较低只能表明在允许廉价即时购电调整时电池方案具有一定适应性。

### 3.4 系统性偏差压力检验

{table(['压力情景','重优化成本（元）','成本变化','同情景节费率'],stress)}

负荷全天增加10%且光伏全天减少10%时，成本变化为 {bad['成本变化率_百分比']:+.3f}%。这类偏差不会像独立误差那样相互抵消。电价整体同比变化时，成本应同幅度变化，其结果同时构成目标函数量纲校验；第二层固定绝对容差可能造成极小数值偏差。

因此，模型对本次零均值扰动的成本表现与对系统性偏差的敏感性必须分别表述。本次是单日合成情景试验，不能外推为全年样本外保证。

**鲁棒性结论：** 独立扰动下重新优化的成本较稳定，但持续性扰动与系统性偏差显著放大成本波动，原电池计划的固定执行能力不足。因此不能将现有确定性模型表述为“具有很强抗干扰能力”；更准确的表述是“在允许重新调度时维持数值可行和经济优势，但固定计划需要补救机制”。

## 四、假设检验与改进方向

### 4.1 充放电效率解释

{table(['单程效率','往返效率','购电成本（元）','相对主方案变化'],eff)}

若90%表示总往返效率，应取两侧效率均为 $\sqrt{{0.9}}$，此时成本为 {alt['成本_元']:.4f} 元，相对主方案变化 {alt['相对主方案成本变化_百分比']:+.3f}%。应核实题意或设备定义后确定主口径，不能为获得更低费用而选择效率解释。

### 4.2 按误差来源改进

1. **持续误差和系统性偏差。** 将负荷、光伏的确定性输入改为含偏差项和随机误差的情景输入；优先使用真实历史预测残差校准偏差、尾部范围和时间相关性，采用连续时间块抽样或联合情景，加入滚动重优化和适度储能安全余量。新增方案需与本模型在同一信息集下比较。
2. **原计划执行出现供需缺口。** 明确可调整购电的时点与费用，在模型中引入调整量、紧急购电及对应真实结算成本。若电网接入受限，再加入 $g_t\le \overline P^g\Delta t$，并报告实际缺供电量及缺供时段；本次未测得接入上限，不能宣称具备该条件下的抗扰能力。
3. **固定效率与忽略衰减不符合设备。** 获得设备数据后，用分段线性关系刻画效率随功率、储电量或温度变化，并计入经标定的循环损耗费用。当前第二阶段减少吞吐量仅是同成本解筛选，不等同于完整电池衰减模型；加入损耗费用会改变目标口径，应另列购电费与综合费用。
4. **免费弃光、不允许售电假设不成立。** 按实际规定增加弃光惩罚或售电收益和容量限制，必要时加入购售电互斥。当前无弃光并不能证明在所有光伏情景下该假设无影响。
5. **检验代表性不足。** 目前只有单日输入的合成扰动试验。后续应按时间顺序扩展到多季节、多典型日，使用真实样本外预测驱动调度，统计成本、缺供、应急购电、后悔值和尾部风险。若另建预测模型，可在该预测模块中采用按时间推进的验证，并报告预测均方根误差，再检验误差对调度费用的影响。

数值回代误差已处于浮点精度量级，没有证据表明需要为了减小此类误差而盲目增加非线性项或集成模型；当前优先级是明确效率与结算假设，改善持续偏差的建模和可执行补救机制。

## 五、代码与复现

完整代码见项目根目录 `problem1_validate.py`，报告生成脚本见 `working/write_problem1_validation_report.py`。在项目根目录执行：

```powershell
python problem1_validate.py
python working/write_problem1_validation_report.py
```

检验脚本依赖原求解器，普通环境安装原项目依赖即可；本次环境使用应用自带解释器，缺少的科学计算依赖安装在外层工作区 `.validation-deps`，脚本会自动发现。本次运行版本：解释器 {s['运行环境']['python']}，数组库 {s['运行环境']['numpy']}，数据框库 {s['运行环境']['pandas']}，科学计算库 {s['运行环境']['scipy']}。输入校验值与检验配置写入 `有效性检验摘要.json`。

明细位于 `results/第一问模型检验/`：有效性检验摘要、随机扰动逐情景结果、随机扰动汇总、系统性偏差压力检验、效率假设检验。原附件、原官方结果工作簿及原逐时段调度未被覆盖。论文正文已接入 `论文草稿/main.tex`，本次未进行排版编译或页面视觉验收。
"""
(ROOT/"docs"/"第一问模型检验与鲁棒性分析.md").write_text(md,encoding="utf-8")


def tex_table(caption, headers, rows):
    def escape(x):
        return str(x).replace('%',r'\%')
    cols = 'l'+'r'*(len(headers)-1)
    return '\n'.join([r'\begin{table}[htbp]',r'\centering\small',
        r'\caption{'+caption+'}',r'\begin{tabular}{'+cols+'}',r'\toprule',
        ' & '.join(map(escape,headers))+r'\\',r'\midrule']+
        [' & '.join(map(escape,row))+r'\\' for row in rows]+
        [r'\bottomrule',r'\end{tabular}',r'\end{table}'])

tex = rf"""% 本节由已运行的检验结果自动生成；原始结果位于第一问模型检验目录。
\section{{第一问模型检验与鲁棒性分析}}
\label{{sec:q1-validation}}

\subsection{{检验对象与方法}}
第一问属于确定性单日调度优化问题，不含需训练的预测器，故不使用随机五折交叉验证或分类一致性指标。随机划分时段还会破坏储能状态连续性。本文采用物理约束独立回代、连续松弛下界、无储能对照，以及扰动条件下的重优化和固定计划执行检验。

检验沿用原模型的单程充放电效率0.9、免费弃光、禁止售电和无额外电网接入容量限制假设。各情景初末储电量均为6000千瓦时，容量与充放电功率边界保持一致。

\subsection{{物理有效性与最优性}}
由原始负荷、光伏功率重新换算时段电量，独立计算母线平衡和储能递推残差：
\begin{{align}}
r_t^B&=g_t+R_t+d_t-L_t-c_t-w_t,\\
r_t^E&=E_t-E_{{t-1}}-\eta_{{ch}}c_t+d_t/\eta_{{dis}}.
\end{{align}}
两类最大绝对残差分别为 ${r['平衡最大残差_千瓦时']:.3g}$ 和 ${r['储能递推最大残差_千瓦时']:.3g}$ 千瓦时，均远小于预设数值容差 $10^{{-5}}$ 千瓦时。母线平衡均方根残差为 ${r['平衡均方根残差_千瓦时']:.3g}$ 千瓦时，仅反映物理平衡精度，不代表预测误差。
充放电功率、储能容量、非负性和弃光上界的最大违反量为零，日末储能偏差为零，同时充放电时段数为零。另检查
\begin{{equation}}
\sum_tg_t=\sum_t(L_t-R_t+w_t)+(1-\eta_{{ch}})\sum_tc_t
+(1/\eta_{{dis}}-1)\sum_td_t,
\end{{equation}}
其残差为 ${r['全天能量恒等式残差_千瓦时']:.3g}$ 千瓦时。原保存调度也通过回代，新旧购电成本一致。

独立建立去除充放电互斥的连续模型，保留其余物理约束，得到原问题最优成本下界。无储能成本为 {s['无储能成本_元']:.8f} 元，连续松弛下界为 {s['独立线性松弛下界_元']:.8f} 元，最终两阶段成本为 {s['基准成本_元']:.8f} 元，节费率为 {s['节费率_百分比']:.4f}\%。最终成本与下界仅差 $10^{{-5}}$ 元，对应第二阶段减少吞吐量时允许的购电费容差，小于 $10^{{-4}}$ 元的检验阈值，支持数值容差内的全局最优性。物理可行性与下界证据共同表明，模型对给定数据和假设有效，但尚非实际预测准确性或所有工况适用性的证明。

\subsection{{输入扰动与重优化}}
设扰动后的负荷及光伏为
\begin{{equation}}
\widetilde P_t^L=P_t^L(1+\alpha\xi_t^L),\qquad
\widetilde P_t^{{PV}}=P_t^{{PV}}(1+\alpha\xi_t^{{PV}}),\qquad
\xi\sim U[-1,1].
\end{{equation}}
取 $\alpha=5\%,10\%,20\%$，其中10\%表示最大相对扰动幅度，非标准差；电价保持不变，夜间零光伏保持为零。逐时段独立组每时段抽样，一小时相关块组每六个连续时段共享误差，负荷与光伏分别独立抽样。随机种子为20260911，每种结构和幅度生成100个情景，共600个；同一结构不同幅度共享基础随机数。

成本变化率定义为 $100(C_\omega^*-C^*)/C^*$。表中区间为100个情景的2.5\%与97.5\%经验分位区间，并非均值置信区间或真实运行保证。
"""
tex += tex_table('随机扰动下重优化成本的相对变化',
    ['结构','幅度','均值','经验分位区间','最大绝对变化'],[v[:5] for v in rand])
tex += rf"""
600个情景均通过求解和独立回代，最大违反量为 ${a['最大回代违反量_千瓦时'].max():.3g}$ 千瓦时。
10\%独立扰动和一小时相关块扰动下，最大绝对成本变化分别为 {ind['最大绝对成本变化_百分比']:.3f}\% 与 {block['最大绝对成本变化_百分比']:.3f}\%。各情景均根据完整扰动路径重新求解，因而属于完全信息下的敏感性试验，不能替代日前固定计划的执行检验。

\subsection{{原计划执行能力}}
首先将原购电、充放电和弃光全部冻结，按扰动数据回代平衡，将负残差累加为待补救缺电量，正残差记录为富余量；此处缺电量并非已经模拟真实停电。
其次只固定电池充放电，允许按原电价即时调整购电与弃光，令
\begin{{align}}
\widetilde g_t&=[\widetilde L_t-\widetilde R_t+c_t^0-d_t^0]_+,\\
\widetilde w_t&=[-(\widetilde L_t-\widetilde R_t+c_t^0-d_t^0)]_+.
\end{{align}}
还需检查 $\widetilde w_t\le\widetilde R_t$；否则在禁止售电条件下原电池放电不可吸收。仅对可行情景计算后悔率 $100(C_\omega^{{fix}}-C_\omega^*)/C_\omega^*$，不可行情景成本和后悔率留空。
"""
tex += tex_table('固定计划的执行检验',
    ['结构','幅度','平均缺电量','电池固定可行率','平均后悔率'],
    [[v[0],v[1],v[3],v[4],v[5]] for v in exe])
tex += r"""
表中缺电量单位为千瓦时；可行率对应固定电池、允许电网补救的条件，后悔率只统计可行情景。本补救假设未计应急溢价或调整惩罚，不能作为后续小问的结算结果。电池调度相对变化另按充放电向量绝对差总和除以基准吞吐量记录于明细。同价时段存在等价最优解时，调度向量变化不宜直接解释为经济性不稳定。

10\%独立扰动下，固定电池方案在100个情景中均不可行；一小时相关块扰动下仅13个可行。某些时段负荷下降导致原定电池放电超过可吸收负荷，在禁止售电、禁止任意丢弃电池放电时，单纯调整购电无法恢复平衡。必须允许电池调度随实际输入调整。该发现针对本次返回的特定最优调度，并非对所有等价最优解的共同证明。

\subsection{系统性偏差与效率假设}
"""
tex += tex_table('系统性输入偏差的压力检验',['情景','成本（元）','成本变化','节费率'],stress)
tex += rf"""
负荷全天增加10\%且光伏全天减少10\%时，成本变化为 {bad['成本变化率_百分比']:+.3f}\%，说明持续偏差无法像零均值独立误差一样相互抵消。电价整体同比变化时，购电成本同幅度变化，也支持目标函数量纲正确。
"""
tex += tex_table('效率解释及参数敏感性',['单程效率','往返效率','成本（元）','相对主方案变化'],eff)
tex += rf"""
若题设90\%为往返效率，则应取 $\eta_{{ch}}=\eta_{{dis}}=\sqrt{{0.9}}$，成本为 {alt['成本_元']:.4f} 元，较主方案变化 {alt['相对主方案成本变化_百分比']:+.3f}\%。主口径应根据题意或设备定义确定，不能以低费用作为选择依据。

\subsection{{改进方向与结论边界}}
\begin{{enumerate}}
\item 对持续误差和系统性偏差，使用真实预测残差校准偏差、尾部及相关性，引入连续时间块或联合情景、滚动优化及储能安全余量，再在相同信息集下与本模型比较。
\item 原计划遇到供需缺口时，应明确调整时点与费用，加入实际结算下的应急购电与调整变量。若存在电网接入上限，增加 $g_t\le\overline P^g\Delta t$，并独立统计缺供量和缺供时段。本次未验证有限接入容量下的适应性。
\item 设备数据若不支持常效率假设，可采用随功率、储电量或温度变化的分段线性效率，并标定循环损耗费用。当前第二层减少吞吐量仅筛选近似同成本解，不能替代完整衰减模型。
\item 若允许售电或存在弃光惩罚，应增加收益、惩罚和容量限制，必要时增加购售电互斥。本次单日无弃光不代表该假设对其他情景均无影响。
\item 扩展至多季节、多典型日真实样本外预测驱动的调度检验，报告成本、缺供、应急购电及尾部风险。若单独建立预测模块，使用按时间推进的验证；不能将本次合成扰动等同于全年泛化检验。
\end{{enumerate}}
本次物理残差已达浮点精度量级，优化有效性得到数值支持。抗扰结论限于本次扰动分布及补救假设；重优化成本稳定不等于原计划可直接执行。优先改进应是持续误差建模、执行补救机制和效率、结算口径的核实。
独立扰动下重优化成本较稳定，但持续性误差与系统性偏差显著增加成本波动，原固定电池计划的执行能力不足，故不将现模型概括为具有很强抗干扰能力。

完整检验代码为项目根目录的\texttt{{problem1\_validate.py}}，运行后由\texttt{{working/write\_problem1\_validation\_report.py}}生成本节；逐情景结果、随机种子、版本与输入校验值均保存在第一问模型检验结果目录。本文尚未完成排版编译与页面验收。
"""
# 将科学计数法转为规范数学排版。
import re
tex=re.sub(r'\$([0-9.]+)e([+-]?\d+)\$',lambda m:'$'+m[1]+r'\times10^{'+str(int(m[2]))+'}$',tex)
(ROOT/"论文草稿"/"第一问模型检验.tex").write_text(tex,encoding="utf-8")
print("已生成中文检验报告与论文检验章节。")
