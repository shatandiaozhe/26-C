# 论文草稿编写说明

## 当前入口与内容

本目录的 `main.tex` 是本次新建的论文源文件。已收录题目、摘要、关键词、问题重述、问题分析、模型假设、符号说明及两条参考文献。

该文件参照 `../archive/旧版论文草稿_勿作为当前成果/main.tex` 的排版设置，使用中文文档类、相同纸张与页边距、分级标题、按章节编号的公式和图表，以及可跨页的三线符号表。

当前题目与摘要仍主要对应问题一，问题重述及后续分析覆盖全题。问题二至四已完成基准求解、结果统计和部分敏感性重算，但模型章节、参数定稿及摘要仍需统一更新。

## 后续输出约定

根据用户本轮明确要求，后续论文步骤继续以本目录 `main.tex` 的格式输出，直接更新源文件，回复中提供文件链接。不要只提供聊天正文，也不要在旧版归档文件中续写。

- 保持现有文档类、页面设置、标题层级、编号规则和中文行文。
- 新增正文放在源文件“后续正文接续位置”，参考文献始终放在完整正文之后。
- 一级、二级、三级标题分别使用 `\section`、`\subsection`、`\subsubsection`。
- 公式使用 `equation`、`align` 等环境，引用使用标签，不手写固定章节或公式编号。
- 表格使用三线表，较长表格沿用 `longtable` 并保留续表表头。
- 图片统一使用图环境；本地图片放在本目录的图片子目录，或引用上一层已有图目录。
- 正文、解释和注释使用中文；必要的排版命令、数学符号及链接按其语法保留。
- 新增符号同步更新全局符号表；新增结果核对原始输出，不引入未经验证的数值。
- 没有实际使用的预设符号应在最终定稿时删除。

## 编译与检查

在含中文字体和所需宏包的完整排版环境中，进入本目录，连续执行两次：

```powershell
xelatex -interaction=nonstopmode -halt-on-error main.tex
xelatex -interaction=nonstopmode -halt-on-error main.tex
```

推荐使用 XeLaTeX 编译器。连续编译两次用于更新交叉引用、文献编号及跨页表格宽度。本稿采用内置参考文献环境，不依赖额外的文献数据库文件。

摘要和问题分析采用局部紧凑排版，并设置独立分页，正文其余部分保留旧稿的默认行距。摘要不超过一页、问题分析不超过一页的要求，仍需在实际编译后的页面中核验。

截至本次整理，当前命令环境未发现可直接调用的中文排版编译器，因此只完成源文件静态检查，不声称已编译成功或已验证页数。

## 尚待定稿的事项

1. 全题完成后，将问题一阶段性题目和摘要改为覆盖全题的版本。
2. 复核问题二至四的结算口径、未利用电量处理、问题四电价信息可得性及每日终端条件；分位数参数定稿后重跑正式结果。
3. 两条文献保留已核验的中文译名、年份、卷页与原文链接；正式定稿前需补齐作者和原文题名，并统一著录格式。
4. 可选流程框架已保留为源文件中文注释，默认不排印；确有需要时再转换为图示。
