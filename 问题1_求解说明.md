# 问题一模型求解说明

## 1. 运行方法

本项目使用 Python 3.11 及以上版本。首次运行前，在本文件所在目录打开 PowerShell：

```powershell
python -m pip install -r requirements-problem1.txt
python problem1_solve.py
```

只想快速验证“数据读取—MILP 求解—约束回代”是否正常，可运行：

```powershell
python problem1_solve.py --smoke
```

主程序会自动读取原始 `附件/附件1.xlsx` 和官方 `附件/附件5/result1.xlsx` 模板；不会覆盖任何原附件。

## 2. 四步求解流程

### 步骤一：数据输入

1. 用 `pandas.read_excel(..., header=0)` 读取附件 1 的 144 条 10 分钟数据。
2. 代码核验四个表头、144 条时间键唯一性、首条 `00:10`、末条 `0:00+1`，以及所有数值非负、无缺失。
3. 将负荷与光伏功率乘以 `1/6` 小时，统一换算为每时段电量（kWh）。

注意事项：

- 原表中的负荷和光伏单位是 kW，优化变量和储能状态单位是 kWh；漏乘 `1/6` 会使结果放大 6 倍。
- 模型内部用时段序号 1—144 对齐数据，写模板时按行号映射，避免中文时间标签导致整体错位。
- 时间标签表示区间起点：首条 `00:10` 对应 `00:10—00:20`，末条 `0:00+1` 对应次日 `00:00—00:10`；四小时汇总按钟表时间循环分组。
- 优化模型保留物理量纲，不对功率、电量做标准化。

### 步骤二：参数初始化

主方案参数如下：

| 参数 | 数值 | 注意事项 |
|---|---:|---|
| 时段长度 | 1/6 h | 与 10 分钟粒度一致 |
| 初始/终端储电量 | 6000 kWh | 0:00 与 24:00 必须相等 |
| SOC 安全范围 | 1200—10800 kWh | 任何时段均不得越界 |
| 最大充/放电功率 | 5000 kW | 每时段上限为 833.3333 kWh |
| 充电效率 | 0.90 | 必须在 `(0,1]` 内调试 |
| 放电效率 | 0.90 | 必须在 `(0,1]` 内调试 |
| 词典序成本容差 | 0.00001 元 | 第二层不得明显牺牲第一层最优成本 |

题面只写“充放电效率为 90%”，主方案按充、放电单程效率各为 0.90 解释，对应往返效率 0.81。若官方定义为总往返效率 0.90，则应运行 `--eta 0.9486832981`；完整程序还会自动把该方案纳入敏感性分析。

### 步骤三：模型调用

程序用 `scipy.optimize.milp` 调用 HiGHS 求解器，变量包括逐时段购电量、充电量、放电量、弃光量、期末 SOC，以及充/放电二元状态。

求解分两层：

1. 第一层最小化全天购电费；
2. 第二层在第一层最优成本容差内最小化充放电吞吐量，减少无效循环。

关键约束包括逐时段供需平衡、SOC 递推、安全边界、充放电功率上限、充放电互斥、禁止售电、允许弃光和日末 SOC 回归。

注意事项：

- 充、放电效率只写入 SOC 递推式，不在供需平衡中重复计算。
- 二元变量严格禁止同一时段同时充放电；只用连续变量虽然更快，但不一定能显式保证互斥。
- 程序设置 60 秒求解时限与 `1e-9` 相对 MIP 间隙；若 HiGHS 未返回成功状态，程序直接报错，不输出伪结果。
- 本模型无随机算法；复现清单仍记录随机种子 0，便于后续统一扩展。

### 步骤四：结果输出

程序输出：

- `results/result1.xlsx`：填入官方模板的完整计划购电量和 6 个四小时充放电汇总；
- `results/问题1_逐时段调度.csv`：144 时段的全部变量与成本；
- `results/问题1_表1指定时段.csv`、`results/问题1_表2充放电汇总.csv`；
- `results/问题1_结果摘要.json`、`results/问题1_两阶段求解记录.csv`；
- `results/问题1_效率敏感性.csv` 与 `results/复现清单.json`；
- `figures/` 下 9 张正式图的 SVG 和 300 DPI PNG，以及 `figures/_qa/` 灰度预览。

注意事项：

- 输出前会回代检查供需平衡、SOC 递推、边界、非负性、互斥和成本基准。
- 使用 `np.sum()`/Pandas `.sum()` 对浮点数组求和，而不手写循环累加，既让单位链清晰，也减少索引遗漏；均值场景应使用 `np.mean()`，避免手写 `sum()/n` 时发生类型或缺失值处理差异。
- 官方模板只写数值并另存到 `results/`，不删除未知工作表、不改原模板。

## 3. 实际运行结果

求解器状态为 `HiGHS Status 7: Optimal`。主方案结果如下：

| 指标 | 结果 |
|---|---:|
| 全天计划购电量 | 59482.6990 kWh |
| 全天计划购电费 | 35126.9486 元 |
| 无储能基准购电量 | 61789.9354 kWh |
| 无储能基准购电费 | 48052.0466 元 |
| 节省金额 | 12925.0980 元 |
| 成本降幅 | 26.90% |
| 全天充电量 | 20740.6660 kWh |
| 全天放电量 | 16799.9395 kWh |
| 弃光量 | 0 kWh |
| SOC 范围 | 1200—10800 kWh |
| 24:00 SOC | 6000 kWh |
| 最大供需平衡残差 | 2.274e-13 kWh |
| 最大 SOC 递推残差 | 8.313e-13 kWh |
| 同时充放电时段数 | 0 |

题目指定时段的购电量：

| 时间段 | 购电量（kWh） |
|---|---:|
| 10:00—10:10 | 0.0000 |
| 12:00—12:10 | 486.4029 |
| 14:00—14:10 | 0.0000 |
| 16:00—16:10 | 394.9315 |
| 18:00—18:10 | 636.9826 |
| 20:00—20:10 | 0.0000 |

四小时充放电汇总：

| 时间段 | 充电量（kWh） | 放电量（kWh） |
|---|---:|---:|
| 0:00—4:00 | 4500.0000 | 0.0000 |
| 4:00—8:00 | 833.3333 | 5947.4196 |
| 8:00—12:00 | 3954.6308 | 2121.4184 |
| 12:00—16:00 | 6119.3685 | 91.1014 |
| 16:00—20:00 | 0.0000 | 5068.6869 |
| 20:00—24:00 | 5333.3333 | 3571.3132 |

## 4. 可视化选择与关键结论

| 类别 | 文件 | 图型与作用 | 图下关键结论 |
|---|---|---|---|
| 原始数据 | `raw_q1_load_pv_price` | 双面板折线图；展示负荷、光伏与电价的时间趋势 | 电价峰谷比 3.76，存在跨时段套利空间 |
| 原始数据 | `raw_q1_netload_distribution` | 直方图；展示净负荷分布和光伏富余 | 共有 30 个光伏富余时段 |
| 原始数据 | `raw_q1_price_netload_scatter` | 散点图；展示电价与净负荷的连续变量关系 | Pearson 相关系数 0.093，需联合考虑供需与价格 |
| 模型过程 | `process_q1_soc_actions` | SOC 折线 + 充放电面积图 | SOC 始终在安全范围且日末回到 6000 kWh |
| 模型过程 | `process_q1_constraint_residuals` | 对数点图；展示约束数值精度 | 三类残差均低于 1e-12 kWh |
| 模型过程 | `process_q1_cumulative_cost` | 累计成本折线图 | 24:00 累计节省 12925.10 元 |
| 最终结果 | `result_q1_purchase_schedule` | 时序折线对比；展示无储能和优化购电策略 | 购电由高价时段转移到低价时段 |
| 最终结果 | `result_q1_cost_comparison` | 柱状图；对比两个方案总成本 | 优化成本降低 26.90% |
| 最终结果 | `result_q1_efficiency_sensitivity` | 参数折线图；分析单程效率影响 | 效率越高，最优购电费越低 |

所有图均含标题、坐标轴标签、图例（适用时）和图下结论；折线使用线型冗余编码，灰度预览可辨。

## 5. 结果解释

最优策略的核心是“低价与光伏富余时充电，高价且光伏不足时放电”。由于日末 SOC 被强制回到 6000 kWh，节省并非来自透支第二天电量，而是来自真实的时移套利和光伏消纳。全天购电量仍高于负荷减全部光伏的代数和，这是往返效率损失所致；回代残差、SOC 边界和互斥检查均通过，说明方案满足题目物理约束。
